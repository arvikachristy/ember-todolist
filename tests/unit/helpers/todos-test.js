import { todos } from 'embertodo/helpers/todos';
import { module, test } from 'qunit';

module('Unit | Helper | todos');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = todos([42]);
  assert.ok(result);
});
